# next@15 canary pins react@19.0.0-rc.0, but @testing-library/react resolves a
# newer react as a peer. The versions are compatible in practice, so relax peer
# resolution. This also keeps `npm install` working on Vercel (bare install).
legacy-peer-deps=true
